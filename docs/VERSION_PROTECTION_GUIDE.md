# Руководство по защите описания при обновлении версии

## 📋 Состояние защиты

Ваш репозиторий `qwen-code` защищен от случайных изменений описания при автоматическом обновлении версии.

### ✅ Установлено:
- ✓ GitHub Actions workflow: `version-sync-protection.yml`
- ✓ Проверка целостности README.md
- ✓ Валидация формата версии (SemVer)
- ✓ Синхронизация CHANGELOG.md

---

## 🔒 Что защищено:

### 1. **Описание в README.md**
```markdown
**AI-powered command-line workflow tool for developers**
```
**Статус:** 🔐 Защищено от изменений

### 2. **Версия в package.json**
```json
{
  "name": "@qwen-code/qwen-code",
  "version": "0.4.0"
}
```
**Статус:** ✓ Валидна (SemVer формат)

### 3. **Скриншот и медиа**
```
./docs/assets/qwen-screenshot.png
```
**Статус:** ✓ Добавлено в repo

---

## 🛡️ Как работает защита:

### Триггеры проверки:
Workflow активируется при изменении:
- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `.github/workflows/release.yml`

### Что проверяется:

1. **Целостность описания**
   - Сравнивает описание в README между веткой и PR
   - Блокирует PR если описание изменилось

2. **Формат версии**
   - Проверяет SemVer формат: `X.Y.Z`
   - Отклоняет неправильные форматы

3. **Синхронизация CHANGELOG**
   - Убеждается что CHANGELOG обновлен для новой версии
   - Выдает предупреждение если записи нет

4. **Структура package.json**
   - Валидирует JSON
   - Проверяет обязательные поля

---

## 📝 Правила при обновлении версии

### Правильный процесс:

```bash
# 1. Создайте ветку для релиза
git checkout -b release/v0.5.0

# 2. Обновите CHANGELOG.md
# Добавьте новый раздел вверху:
## [0.5.0] - 2025-12-14
### Added
- Новая фича 1
- Новая фича 2

### Fixed
- Исправление 1

# 3. Обновите version в package.json
# Вручную измените:
"version": "0.5.0"

# 4. Коммитьте
git add package.json CHANGELOG.md
git commit -m "chore: bump version to 0.5.0"

# 5. Создайте PR
git push origin release/v0.5.0
# На GitHub создайте Pull Request
```

### ❌ Что НЕ ТРОГАЙТЕ:

```markdown
# ❌ Не изменяйте это:
- Описание в README.md: **AI-powered command-line workflow tool for developers**
- Скриншоты и медиа файлы
- Общую структуру документации
```

---

## 🚀 Запуск workflow вручную

### Через GitHub CLI:
```bash
# Просмотреть статус workflow
gh run list --workflow=version-sync-protection.yml

# Просмотреть логи последнего запуска
gh run view --log
```

### Через GitHub Web:
1. Откройте: https://github.com/Killtip74/qwen-code/actions
2. Найдите: "Version Sync Protection"
3. Кликните на последний запуск для просмотра логов

---

## 📊 Статус проверок:

| Проверка | Статус | Описание |
|----------|--------|----------|
| README описание | 🔐 Защищено | Не может быть изменено автоматически |
| Формат версии | ✓ Валидация | Проверяет SemVer (X.Y.Z) |
| CHANGELOG | ⚠️ Предупреждение | Рекомендует обновить вручную |
| package.json | ✓ Валидация | Проверяет JSON синтаксис |

---

## 🔧 Если workflow дал ошибку:

### Ошибка: "README description was changed!"
```bash
# Откатите последний коммит
git revert HEAD

# Верните оригинальное описание
git checkout origin/main -- README.md

# Закоммитьте
git commit -m "revert: restore original README description"
```

### Ошибка: "Version does not follow SemVer format"
```bash
# Убедитесь что версия в формате X.Y.Z
# Неправильно: "0.4" или "v0.4.0"
# Правильно: "0.4.0"

# Исправьте в package.json
"version": "0.4.0"
```

---

## 📞 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте логи workflow: https://github.com/Killtip74/qwen-code/actions
2. Откройте issue с логами ошибки
3. Обратитесь к team lead'у

---

## 📚 Дополнительные ресурсы:

- [SemVer спецификация](https://semver.org/)
- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Conventional Commits](https://www.conventionalcommits.org/)
