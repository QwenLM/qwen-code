# Reference integrations

This directory contains private, independently deployable reference integrations for Qwen Code. They are not part of the published CLI or a public package API.

Each integration owns its runtime dependencies, build, and tests through a dedicated npm workspace. Production deployments must review and supply the external services, credentials, policies, and operational controls described by that integration.
