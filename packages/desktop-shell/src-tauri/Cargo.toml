[package]
name = "qwen-code-desktop"
version = "0.0.1"
description = "Thin desktop shell for Qwen Code Web Shell"
authors = ["Qwen Team"]
license = "Apache-2.0"
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2.4.1", features = [] }

[dependencies]
command-group = "5.0.1"
open = "5.4.0"
rand = "0.9.2"
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.8.5", features = [] }
ureq = { version = "3.1.2", default-features = false }
url = "2.5.4"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
