Merge origin/main into codex/promote-nightly-release-10897

Resolve conflicts from main's release-workflow shell extraction against
the promote_nightly feature:

- Port the promote_nightly mode/ref input guards into run-release-step.sh
  set-flags.
- Wire PROMOTION_SHA through resolve-commit and the promote-nightly
  version args through resolve-version.
- Import assertVersionUnreleased from assert-release-version.mjs for the
  prepare-time already-shipped refusal, keeping the push-time guard on the
  workflow-pinned SHA.
- Map version-resolution refusals (exit 3/4) in runCli so notify_failure
  keeps benign refusals out of the release-failed issue.
- Update release-workflow.test.js to pin extracted-script content and raise
  the orchestration line ceiling for the promotion wiring.
- Record the merged release.yml byte size in .size-baseline.

Co-authored-by: Qwen-Coder <qwen-coder@alibabacloud.com>
Patrol-Run: qwen-pr-conflict/jmtswh6huru
