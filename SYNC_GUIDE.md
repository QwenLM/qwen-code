# 🔄 Руководство по синхронизации с Upstream

Это руководство объясняет, как настроена автоматическая синхронизация вашего форка с основным репозиторием [QwenLM/qwen-code](https://github.com/QwenLM/qwen-code).

## 📋 Содержание

- [Что синхронизируется?](#что-синхронизируется)
- [Автоматическая синхронизация](#автоматическая-синхронизация-github-actions)
- [Ручная синхронизация](#ручная-синхронизация)
- [Часто задаваемые вопросы](#faq)
- [Решение проблем](#решение-проблем)

---

## 📦 Что синхронизируется?

### ✅ Сохраняется (НЕ будет перезаписано)

- `README.md` - ваша локальная версия
- `docs/assets/` - все файлы в этой директории

### 🔄 Обновляется (из upstream)

- Все остальные файлы и директории
- `package.json` (версия пакета может измениться)
- Исходный код (`packages/`, `scripts/`)
- Конфигурационные файлы
- Интеграционные тесты

---

## 🤖 Автоматическая синхронизация (GitHub Actions)

### Как это работает?

GitHub Actions **автоматически проверяет обновления каждый день в 00:00 UTC** (05:00 по Челябинскому времени).

Если найдены новые изменения:
1. ✅ Сохраняются ваши файлы (`README.md`, `docs/assets/`)
2. 🔄 Применяются изменения из upstream
3. 📝 Создаётся Pull Request с изменениями
4. 👀 Вы можете их проверить перед слиянием

### Просмотр активности

1. Перейдите в раздел **[Actions](https://github.com/Killtip74/qwen-code/actions)**
2. Ищите workflow "Sync with Upstream"
3. Посмотрите логи синхронизации

### Запуск синхронизации вручную

Если не хочется ждать следующего дня:

1. Перейдите в **[Actions](https://github.com/Killtip74/qwen-code/actions)**
2. Выберите workflow **"Sync with Upstream"**
3. Нажмите кнопку **"Run workflow"**
4. Выберите ветку `main`
5. Нажмите **"Run workflow"**

---

## 🖥️ Ручная синхронизация

### Если вы работаете локально

Можете синхронизировать на своем компьютере:

#### 1️⃣ Способ 1: Использование скрипта (проще)

```bash
bash scripts/sync-upstream.sh
```

Скрипт:
- 💾 Автоматически сохранит ваши файлы
- ✔️ Попросит подтверждение перед слиянием
- 📊 Покажет статус синхронизации
- 🎯 Создаст ветку `sync-upstream`

#### 2️⃣ Способ 2: Ручная синхронизация (больше контроля)

```bash
# 1. Добавить upstream (если ещё не добавили)
git remote add upstream https://github.com/QwenLM/qwen-code.git

# 2. Получить обновления
git fetch upstream main

# 3. Посмотреть, какие файлы изменились
git diff --name-only main upstream/main

# 4. Сохранить ваши файлы (если нужно)
mkdir -p .backup
cp -r README.md .backup/
cp -r docs/assets .backup/

# 5. Создать ветку для синхронизации
git checkout -b sync-upstream

# 6. Слить изменения
git merge -X theirs upstream/main --no-commit

# 7. Вернуть ваши файлы
cp .backup/README.md README.md
cp -r .backup/assets docs/
git add README.md docs/assets/

# 8. Завершить слияние
git commit -m "chore: sync with upstream QwenLM/qwen-code

Основные обновления:
- Синхронизация с официальным репозиторием
- Сохранены локальные файлы: README.md, docs/assets/"

# 9. Слить в main
git checkout main
git merge sync-upstream

# 10. Отправить
git push origin main

# 11. Удалить временную ветку
git branch -D sync-upstream
rm -rf .backup
```

### Что делать если возникли конфликты?

```bash
# 1. Посмотреть конфликты
git status

# 2. Решить конфликты вручную в редакторе
code .  # или любой другой редактор

# 3. Добавить решённые файлы
git add .

# 4. Завершить слияние
git commit -m "resolve: merge conflicts with upstream"
```

---

## FAQ

### ❓ Может ли это повредить мои файлы?

**Нет!** Структура синхронизации специально разработана, чтобы:
- ✅ Сохранить `README.md`
- ✅ Сохранить `docs/assets/`
- ✅ Создать Pull Request (не автоматический merge)
- ✅ Дать вам время для проверки перед слиянием

### ❓ Что если я хочу добавить ещё файлы в исключения?

Отредактируйте `.github/workflows/sync-upstream.yml` и добавьте в раздел "Restore custom files":

```yaml
- name: Restore custom files
  if: steps.check_diff.outputs.has_changes == 'true'
  run: |
    # Существующие исключения
    cp .backup/README.md README.md
    cp -r .backup/assets docs/
    
    # Добавьте ваш файл
    cp .backup/мой-файл.txt мой-файл.txt
```

### ❓ Как отключить автоматическую синхронизацию?

Просто удалите файл `.github/workflows/sync-upstream.yml`:

```bash
git rm .github/workflows/sync-upstream.yml
git commit -m "chore: disable automatic upstream sync"
git push
```

Вы всё ещё сможете синхронизировать вручную через скрипт.

### ❓ Как часто происходит синхронизация?

**По умолчанию:** Каждый день в 00:00 UTC (05:00 по Челябинскому времени)

Чтобы изменить время, отредактируйте строку в `.github/workflows/sync-upstream.yml`:

```yaml
on:
  schedule:
    # Измените это значение (формат cron)
    - cron: '0 0 * * *'  # Каждый день в полночь UTC
```

[Помощь по cron синтаксису](https://crontab.guru/)

### ❓ Я случайно слил PR, содержащий unwanted изменения. Что делать?

```bash
# Откатить последний коммит
git revert HEAD
git push

# Или сделать hard reset (будьте осторожны!)
git reset --hard HEAD~1
git push --force
```

---

## 🐛 Решение проблем

### Проблема: "Failed to merge"

**Решение:** Могут быть конфликты слияния.

```bash
# Отменить попытку
git merge --abort

# Попробовать снова со своей стратегией
git merge -X ours upstream/main  # Приоритет вашим файлам
```

### Проблема: "Workflow не запускается"

**Решение:** Проверьте:

1. Включены ли Actions: Settings → Actions → General → Allow all actions
2. Есть ли права: Repository должна быть в вашей учётной записи
3. Синтаксис cron верен

### Проблема: "PR создан, но я не хочу его мержить"

**Решение:** Просто закройте PR:

1. Откройте PR в репозитории
2. Нажмите "Close pull request"
3. Ветка будет автоматически удалена

В следующий раз синхронизация снова создаст новый PR.

---

## 📚 Дополнительные ресурсы

- [Официальный репозиторий QwenLM/qwen-code](https://github.com/QwenLM/qwen-code)
- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Git Fork документация](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks)

---

## 📞 Нужна помощь?

Если возникли вопросы:

1. 📖 Проверьте этот гайд
2. 🔍 Посмотрите логи GitHub Actions
3. 🐛 Создайте Issue в репозитории

---

**Последнее обновление:** 2025-12-14

✨ Спасибо за использование этого форка!
