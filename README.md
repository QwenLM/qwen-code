# Local verification screenshots — PR #6625

Screenshots captured while building and running a real `qwen serve` daemon
(bundled Web Shell, driven in headless Chromium) to validate the
multi-workspace management sidebar. Referenced inline from the PR review
comment. This is an assets-only branch (unrelated history) and safe to delete.
