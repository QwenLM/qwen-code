Verification evidence for QwenLM/qwen-code#8980.

- `pr8980-resume-ab.png` — `qwen --resume` over two durable transcripts of the
  same MAX_TOKENS output-recovery turn, rendered by the *same* PR bundle:
  BEFORE = transcript written by the pre-fix build, AFTER = written by the patched build.
- `pr8980-resume-before.png` / `pr8980-resume-after.png` — the individual frames.
