Validation screenshots for PR #9361 (`feat(scheduled-tasks): allow creating a task with an existing session`).

Produced on 2026-08-24 by running the PR head (9fb594c7a3) and its merge-base with main
(4b750b5b13) as real `qwen serve` daemons against a mock OpenAI-compatible endpoint.
